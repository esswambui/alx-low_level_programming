#include "holberton.h"

/**
 * _abs - computes the  absolute value of an  integer
 * @j: integer argument
 *
 * Return: the absolute value of the numberq
 */

int _abs(int j)
{
	if (j > 0)
	{
		return (j);
	}
	else if (n < 0)
	{
		j = (-1) * j;
		return (j);
	}
}
